# =========================================================
# Art -> Clipart 实验配置
# =========================================================
# 只需覆盖与默认配置不同的项
# 用法: python main.py -c configs/art2clipart.yaml --mode source
# =========================================================

# 数据配置
data:
  source: "Art"
  target: "Clipart"

# 训练配置（可选覆盖）
train:
  adapter:
    confidence_threshold: 0.7
